# Railway PostgreSQL bağlantı adresi (Railway panelinden "Connect" > "Postgres Connection URL")
# Örnek: postgresql://postgres:PAROLA@HOST:PORT/railway
DATABASE_URL=

# Sunucu portu (Railway otomatik atar; lokal için 3000)
PORT=3000
